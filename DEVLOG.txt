Phase 0–2 (what I'd build first)
Phase 0 — Scaffold: copy the 3 core files from reference_sources/, create .venv/requirements.txt, app.py skeleton (queue + worker + console loop), system_prompts/, logging to data/agent.log. Accept: app boots, agents init, console loop echoes, 0 admin.

Phase 1 — Perception: lib/winapi.py + DPI awareness, win_list_hwnd, win_get_info, win_focus, win_get_image, win_get_screenshot, vision_analyze. Accept: list windows → focus Notepad → capture it → vision correctly describes its content.

Phase 2 — Actuation: lib/input_backend.py (SendInput + WM_ fallback), mouse + keyboard tools, clipboard. Accept: type text into Notepad, click a menu item, re-capture and verify the result via vision.

Phases 3–6 (UIA click-by-control-name, learning DB + tracker, heartbeat, hardening) follow after Phase 2 verifies.

Two small confirmations before coding:

Copy source: use the identical files in reference_sources/ (I'll leave iNysha untouched), correct? Yes
API keys: reuse the same DeepSeek keys from iNysha's .env by copying into a new ProLight-agent/.env, correct? Yes
Say the word and I'll start Phase 0.


